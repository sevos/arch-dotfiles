[Sleep]
# Use deep sleep (S3) instead of s2idle for better power savings
# This provides traditional suspend-to-RAM functionality
MemorySleepMode=deep

# Optional: Configure hybrid sleep settings
# HibernateMode=platform
# HybridSleepMode=suspend platform

# Optional: Configure suspend state
# SuspendState=mem